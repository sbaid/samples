using System;
using NUnit.Framework;

namespace UnitTests
{
    [TestFixture]
	public class NUnitTestClass4
    {
<#
	int passedTests = 10000;
	int failedTests = 0;
	String padding = "00";
	double timePerTest = 1.5; //in seconds
	int waitTime = (int)(timePerTest*1000);

	int count = 1;
	String testmethodprefix = "NUnitTest_P1_";


#>

		#region PassingTests
<#
	for(int i=0;i<passedTests;i++)
	{
	String methodName= testmethodprefix + count.ToString(padding);
#>
		[Test]
		public void <#=methodName#>()
		{
			//System.Threading.Thread.Sleep(<#= waitTime #>);
			//Assert.AreEqual(1, 1);
		}
<#		
		count++;
	}
#>
		#endregion

		#region FailingTests
<#
	for(int i=0;i<failedTests;i++)
	{
	String methodName= testmethodprefix + count.ToString(padding);
#>
		[Test]
		public void <#=methodName#>()
		{
			//System.Threading.Thread.Sleep(<#= waitTime #>);
			Assert.Fail();
		}
<#		
		count++;
	}
#>
		#endregion
    }
}
